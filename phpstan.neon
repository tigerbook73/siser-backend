includes:
  - ./vendor/larastan/larastan/extension.neon

parameters:
  paths:
    - app
    - config
    # - database

  # The level 9 is the highest level
  level: 5

  # ignoreErrors:
  # - "#PHPDoc tag @var#"

  excludePaths:
    - ./*/*/FileToBeExcluded.php

  editorUrl: "vscode://file/c:/work/future/code/siser-backend/%%relFile%%:%%line%%"
  editorUrlTitle: "%%relFile%%:%%line%%"
